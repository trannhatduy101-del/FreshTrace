# ─── Deployment ────────────────────────────────────────────────────
# Private key of the wallet that will deploy the contract.
# Leave blank if only running tests / local Hardhat.
# Get free Amoy POL: https://faucet.polygon.technology
PRIVATE_KEY=

# RPC endpoint for the network you deploy to.
# Recommended free Amoy RPC: https://polygon-amoy-bor-rpc.publicnode.com
POLYGON_AMOY_RPC_URL=

# ─── Wallet ────────────────────────────────────────────────────────
# Your MetaMask wallet address (the one you connect to the frontend with).
# Used by scripts/setup.ts to grant all 4 roles to your wallet on deploy,
# and by scripts/fund.ts to send you 10 test ETH on the local Hardhat node.
# Format: 0x... (42 chars)
WALLET_ADDRESS=

# ─── Local Hardhat dev (defaults fine for most users) ──────────────
HARDHAT_RPC_URL=http://127.0.0.1:8545
# Public Hardhat account #0 key — safe to commit, works only on local node.
HARDHAT_PRIVATE_KEY=0xac0974bec39a17e36ba4a6b4d238ff944bacb478cbed5efcae784d7bf4f2ff80

# ─── Optional: deploy.ts multi-wallet variant ──────────────────────
# Only needed if you use scripts/deploy.ts to grant each role to a separate wallet.
TEST_PRODUCER=
TEST_LOGISTICS=
TEST_RETAILER=
TEST_AUDITOR=

# ─── Optional: post-deploy operations ──────────────────────────────
# Used by scripts/grantRoles.ts to grant roles on an already-deployed contract.
CONTRACT_ADDRESS=

# ─── Pinata IPFS (only needed for off-chain script uploads) ────────
# Frontend Pinata config lives in frontend/.env (VITE_PINATA_JWT).
PINATA_API_KEY=
PINATA_SECRET_KEY=
